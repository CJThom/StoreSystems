package com.gpcasiapac.storesystems.common.presentation.navigation

/**
 * Marker interface for feature-local navigation keys.
 * These keys should be pure Kotlin (no UI dependencies) and ideally immutable.
 */
interface FeatureKey